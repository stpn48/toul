import React from "react";

type Props = {
  params: {
    id: string;
  };
};

export default async function ExerciseDetailsModal({ params }: Props) {
  //TODO: Show graph

  return <h1>Exercise Details</h1>;
}
